const enum Color {
    Red = 'red',
    Green = 'green',
    White = 'white',
    Blue = 'blue'
}

let selectedColor: Color = Color.Red; // Assigning Green from the Color enumeration

console.log("Selected color:", selectedColor); 
